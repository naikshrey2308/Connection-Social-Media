const Indicator = [
    {
        sent : 0,
        recieved : 1
    },
    {
        sent : 1,
        recieved : 0
    }
]

// in chats.js => flag = 0 : user1 has sent the msg => flag = 1 : user1 has recieved the msg
module.exports=Indicator;